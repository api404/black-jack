import { FC } from "react";

export const Loading: FC = () => (
  <div className="flex flex-grow items-center justify-center">
    <p className="uppercase text-2xl text-center">Loading 😅</p>
  </div>
);
